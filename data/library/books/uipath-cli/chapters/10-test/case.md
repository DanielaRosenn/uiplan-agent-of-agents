# uip test case

Manage test cases and sets.

## Synopsis

```bash
uip test case list --project <name>
uip test case create --project <name> --title <title> [--steps <file.md>]
uip test set create --project <name> --name <name> --cases <id,id,...>
```
