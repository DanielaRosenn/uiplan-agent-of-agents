# uip platform is

Manage Integration Service connections.

## Synopsis

```bash
uip platform is connector list
uip platform is connection list [--connector <name>]
uip platform is connection create <connector> --name <name> [--config '<json>']
```
