# Azure DevOps wiki landing — paste-ready copy

Page title: **UiPath Claude Code**
Location: Cato Azure DevOps wiki, under the RPA section.

---

## UiPath Claude Code

**Claude Code for UiPath — an agentic CLI and Cursor integration that builds, validates, and runs RPA workflows.**

RPA developers spend hours scaffolding projects, hand-writing XAML, and chasing validator errors. UiPath Claude Code runs that loop for you: it scaffolds the project, writes the XAML, runs the UiPath validator, fixes what it breaks, and only stops to ask when a human decision actually matters.

- **Repo:** `<link to Azure DevOps repo>`
- **Quickstart:** `<link to repo README#quickstart>`
- **Install & full setup:** `<link to repo docs/INSTALL.md>`
- **Architecture:** `<link to repo docs/ARCHITECTURE.md>`
- **Who to ask:** RPA CoE

### What you get

- Validated UiPath projects from a plain-English description.
- BA → SA → Dev → QA bootstrap from a one-paragraph brief.
- Works in the CLI and inside Cursor (skills auto-register).
- Safe by default — tool profiles and approval gates gate anything destructive.
