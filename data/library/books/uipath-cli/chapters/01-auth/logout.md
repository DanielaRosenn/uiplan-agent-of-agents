# uip auth logout

Clear cached tokens for the current or all profiles.

## Synopsis

```bash
uip auth logout [--all]
```

## Flags

- `--all` — remove tokens for every cached profile.

## Examples

```bash
uip auth logout
uip auth logout --all
```
