# InvoiceProcessor Runtime Reliability Tasks

## T001 Scaffold Project

- [x] Create `InvoiceProcessor` with `uip rpa create-project`.
- [x] Record scaffold evidence in `evidence/scaffold.json`.
- [x] Keep `project.json` generated by Studio tooling.

## T002 Ground Activities

- [x] Resolve `Log Message`, `Create Directory`, `Read Text File`, and `Write Text File`.
- [x] Capture default XAML evidence for concrete UiPath activities.
- [x] Avoid `InvokeCode`; use pre-built activities and C# expression bindings.

## T003 Build Workflow

- [x] Enumerate `Data/Input/*.txt`.
- [x] Read each fixture using `Read Text File`.
- [x] Extract and validate fields with explicit `CSharpValue` expressions.
- [x] Write `invoice-report.csv` and `smoke-result.json` with `Write Text File`.

## T004 Validate Locally

- [x] Run `uip rpa get-errors --file-path Main.xaml`.
- [x] Run `uip rpa build`.
- [x] Run `uipcli package analyze`.
- [x] Run `uip rpa run-file --file-path Main.xaml`.
- [x] Assert expected CSV rows and log markers.

## T005 Tenant Smoke

- [x] Check for tenant credentials by presence only.
- [x] Record `out/tenant-blocker.json` because no non-Production tenant credentials were
  available in this session.
