# InvoiceProcessor Fixture Spec

This project-level UiPlan spec mirrors `framework/tests/fixtures/uiplan_runtime_reliability/spec.md`.

- Use a scaffolded UiPath RPA project.
- Prefer pre-built activities over `InvokeCode`.
- Process deterministic invoice text inputs.
- Produce CSV and smoke JSON outputs.
- Prove Studio validation, build, analyzer, local run, and tenant blocker evidence.
