# uip df entity

Manage Data Fabric entities (schemas).

## Synopsis

```bash
uip df entity create <name> --schema <file.json>
uip df entity list
uip df entity describe <name>
uip df entity delete <name>
```

## Examples

```bash
uip df entity create Invoice --schema invoice.schema.json
```
