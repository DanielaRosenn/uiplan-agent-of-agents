# Archive

Historical, customer-specific, or one-off artifacts kept out of the
primary docs surface. Read-only; do not link from active docs without
copying salvageable bits forward.

| Path | What it was |
|---|---|
| `pdd/Escalations-alerts-PDD.md` | Customer-specific PDD example (not template content) |
| `deploy/Escalations-alerts-Deployment-Doc.md` | Customer-specific deployment doc paired with the above |
| `E2E_AUTOMATION_DEMO.md` | One-off demo writeup superseded by `../uiplan/EXPLORER_NEW_PROJECT.md` |

Removed in the 2026-05 cleanup (recoverable from git history):

- `superpowers/specs/` and `superpowers/plans/` - completed brainstorm
  specs and implementation plans for shipped iterations
  (uiplan-runtime-restructure, framework-structure-migration,
  uiplan-studio explorer/builder/generation-graph).
- `input.txt` - chat scratch file accidentally tracked.
