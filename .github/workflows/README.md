# Workflows (disabled in this branch)

No GitHub Actions run here by default. Use **Cursor session hooks** (`.cursor/hooks.json`), **`ops/scripts/update-skills.*`**, and **`uipath-claude doctor`** for local checks; use your OS scheduler for timed jobs.

When you want CI again, add workflow YAML files under this folder and enable Actions on the repository.
