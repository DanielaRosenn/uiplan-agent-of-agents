# uip platform process

Deploy packages as processes and start jobs.

## Synopsis

```bash
uip platform process deploy <package.nupkg> --folder <name>
uip platform process start <process-name> --folder <name> [--input '<json>']
```
