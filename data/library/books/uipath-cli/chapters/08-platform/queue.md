# uip platform queue

Manage queues and queue items.

## Synopsis

```bash
uip platform queue create <name> --folder <name>
uip platform queue add-item <name> --folder <name> --data '<json>' [--reference <ref>]
uip platform queue list-items <name> --folder <name> [--status New|InProgress|Failed|Successful]
```
